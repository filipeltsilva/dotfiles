#!/usr/bin/env bash

echo "##################"
echo "### ASDF SETUP ###"
echo "##################"
echo ""

if [[ ! -d $HOME/.asdf ]]
then
  git clone https://github.com/asdf-vm/asdf.git "$HOME"/.asdf

  ln -sfnv "$HOME"/.dotfiles/asdf/.asdfrc "$HOME"/.asdfrc
  ln -sfnv "$HOME"/.dotfiles/asdf/.default-gems "$HOME"/.default-gems
  ln -sfnv "$HOME"/.dotfiles/asdf/.default-npm-packages "$HOME"/.default-npm-packages
  ln -sfnv "$HOME"/.dotfiles/asdf/.tool-versions "$HOME"/.tool-versions
else
  echo "Nothing to do here!"
fi
