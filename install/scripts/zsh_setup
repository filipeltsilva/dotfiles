echo "ZSH SETUP"
if [[ ! -d $HOME/.antigen ]]; then
  git clone https://github.com/zsh-users/antigen.git $HOME/.antigen
else
  echo "Antigen is already installed in this system"
fi

chsh -s $(which zsh) $USER
